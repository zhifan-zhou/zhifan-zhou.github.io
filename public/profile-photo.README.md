# Profile Photo

Save the portrait photo as:

```text
public/profile-photo.png
```

The site uses CSS `object-fit: cover` and `object-position: 44% 34%` so the face stays centered in the portrait crop. Adjust `profile.photo.objectPosition` in `src/data/profile.ts` if you replace the image with a different crop.
